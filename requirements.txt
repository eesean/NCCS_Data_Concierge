# Core API & frontend
fastapi==0.128.8
uvicorn==0.39.0
streamlit==1.50.0
requests==2.32.5
python-dotenv==1.2.1
pandas==2.3.3
plotly==5.24.1

# Local LLM (Ollama)
ollama==0.6.1

# LangChain (used for ChromaDB + HuggingFace embeddings only — NOT for LLM or agent)
langchain==0.3.27
langchain-community==0.3.31
langchain-huggingface==0.3.1

# langchain-openai is no longer used by the pipeline but kept for evaluation tooling
langchain-openai==0.3.35

# Embeddings & vector store
sentence-transformers==5.1.2
chromadb==1.5.0

# SQL
duckdb==1.4.4
sqlglot==28.10.1

# Pin transformers to avoid Python 3.9 + PyTorch 2.5+ OSError on startup
# transformers >= 4.47 imports torch._dynamo which fails inspect.getsource on Python 3.9
transformers==4.46.3
